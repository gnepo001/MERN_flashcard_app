import { makeStyles } from "@material-ui/styles";

export default makeStyles((theme) => ({
  Loading: {
    marginTop: "100px",
    width: "10%",
    margin: "auto",
    textAlign: "center",
  },
  FlashcardContainer: {
    marginTop: "25px",
  },
}));
