import { makeStyles } from "@material-ui/styles";

export default makeStyles((theme) => ({
  flashcard_card: {
    marginTop: "10px",
    maxWidth: "50%",
    margin: "auto",
  },
  flashcard_button: {
    color: "black",
  },
}));
