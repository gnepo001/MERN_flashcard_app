import { makeStyles } from "@material-ui/styles";

export default makeStyles((theme) => ({
  flippableCard_loading: {
    marginTop: "10px",
    margin: "auto",
    textAlign: "center",
  },
  flippableCard_card: {
    width: "100%",
  },
}));
